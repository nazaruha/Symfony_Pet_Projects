{% extends 'base.html.twig' %}

{% block title %}
    Movies Page
{% endblock %}

{% block body %}
    <h1>{{ name ? name: 'NoMovieName' }}</h1>
    <ul>
        {% for movie in movies %}
            <li>{{ movie.name }} - {{ movie.producer }}</li>
        {% endfor %}
    </ul>
{% endblock %}